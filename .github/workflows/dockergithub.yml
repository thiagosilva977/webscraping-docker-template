name: My Docker Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: docker build
      run: |
        docker build ./webscraping-docker-template -t thiago977/webscraping-template:latest
